# LatentMind V6 — agentic analytics pipeline

# Core inference
torch>=2.1.0
transformers>=4.46.0
accelerate>=0.27.0
tokenizers>=0.19.0

# RAG encoder + latent planner (BGE-M3)
sentence-transformers>=3.0.0
FlagEmbedding>=1.2.0

# Agent graph + conversation memory
langgraph>=0.2.0

# Capabilities: report templates + charts
jinja2>=3.1.0
matplotlib>=3.7.0

# 4-bit quantization (optional, recommended on a T4)
bitsandbytes>=0.43.0

# Voice layer — STT (front) + TTS (back)
# faster-whisper: CTranslate2 Whisper (large-v3) for transcription
# coqui-tts: maintained XTTS-v2 fork (PyPI package name is `coqui-tts`, not `TTS`)
# soundfile: WAV read/write for fixtures and synthesised speech
faster-whisper>=1.0.0
coqui-tts>=0.24.0
soundfile>=0.12.0
# rapidfuzz: fuzzy post-correction of wilaya names + KPI acronyms in transcripts
rapidfuzz>=3.0.0

# MySQL backend (local only — Colab uses SQLite)
mysql-connector-python>=8.0.0

# API server (optional — the `serve` cell on Colab via ngrok). Streams the
# thinking trace + answer text + cloned-voice audio over a WebSocket.
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9   # /ask_voice file uploads
pyngrok>=7.0.0            # public tunnel from Colab
nest-asyncio>=1.6.0       # run uvicorn inside the Colab event loop

# Flash Attention 2 (optional — needs a C++ build, install separately):
#   pip install flash-attn --no-build-isolation
